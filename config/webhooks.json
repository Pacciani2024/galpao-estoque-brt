{
  "webhooks": [
    {
      "_comment": "Receptor do BRT Desenrola. URL via env var BRT_WEBHOOK_URL (configurar no Railway).",
      "url": "${BRT_WEBHOOK_URL}",
      "events": ["event.dispatched", "event.returned", "event.pending"]
    }
  ]
}